# 无脸皮肤重建：最多六人测试版本

## 六人同时入场（当前默认）

- 检测配置与特效管理器的上限均为 6，默认同时处理最多 6 张有效人脸。
  摄像头中超过 6 人时，额外的人不会保证获得检测或特效；本版不处理第 7 张脸。
- 每个人独立执行约 2 秒入场，拥有自己的关键点平滑、实时肤色取样、重建纹理、
  颜色历史与后续生长进度。后来进入的人从原始五官开始渐变，已完成的人保持最终皮肤。
  所有肤色重建始终读取同一张未经特效修改的配对摄像头帧，不读取其他人的合成结果。
- `MidFaceEffect` 自动创建和管理各个人脸图层，无需复制场景或手动添加 6 个组件。
  原有十五个局部区域、内部补全、最终覆盖轮廓、羽化与颜色参数保持原值。
- 跟踪以位置、大小和短时运动预测作整体匹配，不依赖检测数组的先后顺序。
  每个临时轨迹有自己的 `TrackId`；它不代表身份识别。交叉、完全遮挡或几何相近时
  仍可能交换轨迹，需要在实际多人走动中检查。
- 单个人短暂丢失时，保留其轨迹和入场计时，默认超过 3 秒才释放旧轨迹并在重获时重播。
  新配对画面中看不到某个人时，立即隐藏这个人的旧图层，其他人继续正常显示；
  重获后重新采样当前肤色，避免把丢失前的图像历史带到新姿态。
  若 6 个槽位都已占用，新检测会优先接替未匹配的旧槽位，重置该槽位的全部个人状态。
- 只有所有人都丢失时，默认配对预览才沿用整幅最后处理画面的短暂保持与淡黑；
  单个人离开不会让其他人的画面变黑。这不是完全遮挡下的五官隐私保证：
  多人画面中漏检者的旧图层会被隐藏，该位置仍可能显示摄像头原图。
- 只在新摄像头帧与检测结果配对到达时重建皮肤；每个人的透明图层裁剪到自己的
  人脸边界框，避免对 6 张完整屏幕重复合成。原视频和覆盖边缘保持原生分辨率。
  重叠人脸按投影面积估计前后关系，大脸排在小脸前，并恢复前景脸的原始像素；
  这是近似遮挡顺序，不是逐像素深度或手、头发、身体分割。
- 小面板显示 `Faces / 当前人数 / 6` 和配对帧率 `Camera / fps`。
  “最多 6 人”是功能上限，不是 6 人稳定满帧保证；实际速度取决于摄像头分辨率、
  人脸大小、CPU/GPU 与平台，需要在展览电脑实测。降低人数配置也可以用于比较性能。
- 本轮六人 C# 行为、GPU 合成与 Unity/Metal 实机检查状态见末尾“已做检查与限制”；
  当前文档不将上一轮单人测试结果视为六人验收。

## 两秒入场（当前默认）

- 场景默认启用入场动画。第一帧有效人脸保持原画面，随后约 2 秒平滑过渡到
  已确认的最终皮肤效果；十五个局部区域和内部补全先合并，再统一渐入。
  不改变最终轮廓、取色、高光处理、侧脸覆盖或羽化参数。
- 本次渐变是原摄像头与实时重建皮肤之间的连续融合，五官逐渐失去对比度；
  没有新增大半径视频模糊或逐步扩大的蒙版。
- 各个人脸短暂丢失检测时暂停自己的进度，连续超过 3 秒未匹配到该轨迹后，
  再次检测到时从头播放。多人画面与全员丢失的显示行为见上节。
  长时间背对摄像头也会被视为离开；当前检测器不辨认具体身份，不能区分换人
  和同一人长时间转身，短时间在相同位置换人也可能被匹配到旧轨迹。
- `MidFaceEffect` → `Entry Duration Seconds`（入场时长，秒）默认 2；
  `Reset After Absence`（丢失检测后重播阈值，秒）默认 3。
  `Replay all entries (R)` 重播所有已保留轨迹，`Final skin` 立即显示所有人的最终效果，
  H 隐藏控制面板。
- 上一轮单人版本已在 Mesa 上执行项目片元程序，核对 0、0.25、0.5、1、1.5、1.75、2、3 秒
  八个合成时刻：开始为原图，2 秒后等于原最终效果，区域外像素不变。
  已审查入场计时、短暂丢失和重播路径；尚未运行 Unity / Metal / 真人摄像头。
- 以下早期更新记录中的 15 秒分区时间线已由本次两秒整体渐变取代。

## 内部接缝补全

- 保留十五个局部区域的位置、大小和外侧羽化。仅填补被不透明核心围住、
  无法连通外部的内部孔洞；不放大外轮廓，也不使用 Face Oval 填满整张脸。
- 修复了显示覆盖与取色规则之间的差异：某些鼻旁接缝的合成覆盖已超过 99%，
  重建时却仍以较高权重采入该处原始像素，造成移动时残留亮点。
  内部补全现在同时作用于最终合成、肤色取样和重建来源排除。
- 对未被保守不透明核心完整包围的细软接缝，不强行扩大覆盖去封口；联合覆盖达到
  95% 时，禁止该位置的原五官/高光参与取色和重建，85%–95% 平滑过渡。
  这项规则只改变来源权重，外侧合成透明度不变。
- 填孔只分析当前关键点形成的核心几何，不读取摄像头像素到 CPU。
  使用复用缓冲、保守核心栅格及八邻域外部连通检查；相同几何直接复用结果。
  纹理插值的过渡只留在原本已不透明的核心内，外侧软边保持原算法。
- 补洞区与周围皮肤使用统一的颜色时间平滑，避免转头或曝光变化时产生
  小方块、嘴下弧形色阶。旧版仅在补洞区跳到当前颜色，会把补洞纹理边缘显出来。
  现在对历史 RGB 偏差连续限幅（小于 0.008），同时抑制旧亮点和色斑残留；
  不改变当前重建颜色的空间渐变、覆盖范围或羽化。入场时所有区域仍一起渐入。
- 组件与纹理由现有 `MidFaceEffect` 自动管理，无需拖入新组件。
  更新当前分支后，等待 Unity 导入完成即可播放。

## 面中与下巴高光更新

- 增加左右上内颊、左右下巴内侧四个局部软区域，连接原有五官和折痕覆盖，
  处理眼下、鼻翼外侧及下巴两旁残留的亮斑。它们随各自关键点转动和收缩，
  正面不使用整张脸的放大蒙版，外侧脸颊和下颚角仍保留原画面。
- 重建取色仍来自当前摄像头。对于明显高于附近肤色光照的局部亮点，
  在重建来源中使用平滑的高光压缩，减小亮斑被扩散进皮肤的机会；
  阴影和普通明暗渐变保持原处理方式，避免变成统一填充色。
- 场景已启用 `Highlight Suppression`（高光抑制），默认 0.85。
  无需手动添加组件。需要对比时，可在 `MidFaceEffect` 检查器中设为 0。
- 当前入场时所有区域一起渐变，2 秒达到最终效果。
- 本次更新尚未在用户电脑的 Unity / Metal / 摄像头环境验收。

## 法令纹、木偶纹与局部颜色更新

- 在原有七个五官区域之外新增四个独立软区域：左右法令纹、左右嘴角下方木偶纹。
  它们跟随自己的关键点移动和旋转，不通过放大整张遮罩来扩展范围。
- 取色一直来自实时摄像头。本版将六个取色位置改为左右脸颊的上、中、下部，
  并对每个采样像素验证是否位于可信皮肤，避免使用眼部、嘴部和已覆盖折痕。
- 六处颜色按空间位置拟合局部光照，保留两侧与上下部的明暗、冷暖变化。
  原来的滤波流程现在只重建相对于这层光照的色差，再把局部光照加回，
  避免大块缺失区域被平均成同一颜色。覆盖边缘仍由附近的真实皮肤约束。
- 眉上取样范围保留一条安全皮肤带，帮助上边缘接近邻近额头颜色。
  不从发际线之外取样；头发遮到脸内仍属于需要进一步分割处理的情况。
- 默认人工高光 Volume 改为 0，避免额外亮度形成贴片边缘。
  若手动增加 Volume，它会在羽化边缘前渐退。
- `MidFaceEffect` 检查器的 `Local Color Strength` 默认 1；设为 0 可对比关闭
  局部光照趋势的效果。时间平滑继续启用，避免颜色随逐帧检测跳动。
- 上一版的左右转头效果已由用户实测接受；本次范围和颜色更新仍需摄像头验收。

## 侧脸覆盖与配对预览修正

- 最终合成不再用 Face Oval 裁剪。它会在侧脸时把凸出的鼻子、嘴唇甚至
  可见眼部裁掉。现在用 468 点的投影三角形表面限制覆盖，仍然只有
  局部五官和折痕区域产生特效；不是一张肤色网格盖住整张脸。
- 表面限制以摄像头原生分辨率绘制，在真实轮廓处仅做约一像素抗锯齿。
  脸部内部继续使用宽羽化，头发、耳朵、外侧脸颊与下颚没有新增的覆盖区域。
- 重建纹理的坐标范围包含所有投影点。侧脸时两侧脸颊点靠近，也不会让
  鼻尖和嘴唇落到重建纹理之外。这里改变的是纹理坐标范围，不是扩大蒙版。
- 默认开启 `Solution` 上的 `Synchronize Preview With Detection`：每一帧
  先捕获、检测，再把对应画面与关键点一起显示。它使用连续 VIDEO 检测，
  仍然是实时摄像头，但会增加少量延迟；CPU 检测时帧率取决于电脑。
- 配对模式将关键点平滑造成的偏移限制在 0.35 像素，重建颜色继续做时间平滑。
- 所有人完全丢失检测时，先保留最后处理画面，约 0.18 秒后用 0.25 秒淡到黑色；
  重新识别后淡入已处理画面。这个全员丢失路径不会通过撤去遮罩来显示旧帧的原始五官。
  多人模式中单人丢失的图层隐藏与独立计时，以本文件首节规则为准；R 重播所有入场。
- 场景中的旧球体 `facemasktest` 已停用，不需要手动删除。

## 粉屏与读取失败修正

- 自定义亮度函数已改名为 FacelessSkinLuma，避开
  [UnityCG 内置 Luminance](https://docs.unity3d.com/6000.0/Documentation/Manual/SL-BuiltinFunctions.html)。
  独立片元测试现在也注入这个内置函数名，防止重名问题再次漏检。
- 编辑器会检查 Shader 编译错误，将具体消息输出到控制台；
  检测到错误时恢复原始视频材质。首个人脸皮肤帧准备好之前不接管视频材质。
- MediaPipe 异步读取失败时释放缓存帧并自动改用同步 CPU 读取，
  避免错误累积后耗尽纹理帧池。同步读取可能降低帧率。
- 本轮仍未在用户的 Mac/Metal 环境执行，不能仅凭截图断言粉屏原因已全部排除。

## 打开与测试

1. Unity 停止播放；GitHub Desktop 选中 9.6ar-gpt，
   在 Current Branch 中选择 codex/skin-reconstruction-v2 测试分支，
   点击 Fetch origin，再点击 Pull origin 拉取新版本。
2. 回到 Unity 6000.0.58f2，等待导入。若提示场景变更，选择 Reload。
3. 打开 Assets/MediaPipeUnity/Samples/Scenes/Face Landmark Detection/Face Landmark Detection.unity。
4. 点击播放（Play），允许摄像头。MidFaceEffect 已连接三个 Shader，无需手动拖拽。
5. 默认最多 6 人各自在被识别后播放 2 秒入场，再保持最终皮肤。面板应显示 `Faces / 当前人数 / 6`；
   新参与者加入不会重置其他人的入场。右上角 Erase 滑块可以对比原画面。
6. Final skin 显示所有人的最终效果；Replay all entries / R 重播所有渐变；H 隐藏或显示小面板。
   快捷键需要先点击游戏（Game）画面取得焦点。
7. 468 landmarks 显示原始跟踪点；Regions + boundary 显示局部区域、中心点、羽化边缘和投影表面边界。
   `Camera / fps` 显示新配对帧到达频率，不等同于显示器刷新率。

先用 1 人比较正脸和轻微左右转头，再依次增加到 2、4、6 人。检查不同人同时进入、
先后进入、离开、交叉换位时，肤色与渐变是否仍跟随对应人脸，并记录 6 人实际帧率。
本轮多人改动尚未经过用户电脑的 Unity / Metal / 摄像头运行验证。
不要把代码检查通过等同于已经达到参考图。

## 这一版的处理方法

- 配对摄像头帧由原 RawImage 以原分辨率显示。只替换局部特效涉及的颜色。
- 十五个局部区域覆盖眉眼、眉间鼻梁、鼻子、鼻唇之间、嘴、中央下巴、四条折痕，
  以及左右上内颊和下巴内侧的亮斑区域。
  眉眼范围包含眼眶下缘；嘴部根据外唇关键点定义，闭嘴时不会塌成细线。
- Face Oval 只保留为重建来源的取样限制，不再限制最终覆盖。
  最终覆盖与真实关键点投影的三角形并集相交，避免把鼻嘴裁掉，或用凸包填平侧脸凹陷。
- 在滤波前将五官区域的来源权重设为零，避免把眼睛、牙齿、鼻孔扩散成重影。
- 六处脸颊小区域用于拟合局部低频光照和采样有效性判断；不把六种颜色平均成一块填充色。
- GPU 浮点置信度金字塔、连续可分离 Gaussian、逐级回填和边界松弛重建可信皮肤相对光照的残差。
- 重建皮肤在跟随脸部的坐标系里进行短时平滑。原视频和最终遮罩边缘不降采样。
- 每个人的各局部区域先合并透明度，再在自己的裁剪图层里合成，避免区域叠色接缝。
  个人图层按近似前后关系排列；进入中的前景人脸不会透出后方人脸的特效。
- 封闭的内部接缝额外补满，并从取色与重建来源中排除；外侧羽化保持原值。
- 默认关闭人工高光、保留低幅度颗粒，可以在 MidFaceEffect 检查器中调整 Volume / Fine Grain。
- 组件禁用时恢复视频材质及关键点可见性，释放 RenderTexture 和临时材质。

## 入场与后续植物接口

每个人的入场默认独立开启，从该轨迹第一帧可用人脸开始计时；短暂失去跟踪时暂停：

| 时间 | 变化 |
| --- | --- |
| 第 0 秒 | 原始人脸 |
| 0–2 秒 | 全部现有区域和内部接缝一起平滑融合成重建皮肤 |
| 第 2 秒起 | 保持已确认的最终皮肤效果，该轨迹的 entry 达到 1，可开始植物生长 |
| 2–11 秒 | GrowthProgress 从 0 到 1，供后续植物驱动 |

改变入场时长后，每个人植物接口的起点同步移动到自己的入场结束时。

这版未加入植物模型或生长渲染。已预留：

- `MaximumFaces = 6`、`ActiveFaceCount`、`FrameUpdated` 事件。
- `TryGetFaceState(int slot, out int trackId, out float entry, out float growth)`：遍历槽位 0–5，
  返回 true 时可使用该轨迹的独立入场与植物进度。槽位可以重用，必须同时保存并比较 `trackId`；
  ID 改变时销毁或重新初始化旧植物，不能把上一人的模型与生长进度交给新人。
  false 表示当前没有可用跟踪状态，调用方应暂停新锚点更新并自行淡出或枯萎已有植物。
- `TryGetSurfaceAnchor(int slot, int landmarkId, out Pose pose, out float faceWidthWorld)`：
  获取指定当前可见轨迹的锚点；返回 false 时不要沿用它作为新测量。
  建议锚点 168（鼻梁）、6（两眼之间）、0（嘴上方）。
  位置映射到实际视频表面，旋转来自 MediaPipe 头部姿态并按预览镜像修正。
  植物网格朝向、深度和比例仍需导入模型后校准。
- 兼容旧单人代码的 `PresentationSeconds`、`EntryProgress`、`GrowthReady`、`GrowthProgress`、
  `HeadPose` 和不带 `slot` 的 `TryGetSurfaceAnchor` 仍保留，取当前第一个可用轨迹。
  它可能随人离开而改变，不能用于管理六个人的植物。
  `IsTracking` 表示当前至少一人可用；`FacePresence` 是这个整体状态的 0/1 值。
- 默认配对模式只有全员丢失才淡黑；每人重获/重播独立，R 则重播所有人的渐变。

## 已做检查与限制

本轮六人更新的验证状态：

- `Tools/Faceless/verify_tracks.cs` 直接测试生产 `FacelessTrackAssigner.cs`，包含独立 Unity 数学替身，
  检查 6 人顺序变化、短暂丢失与超时、满员时新人的槽位替换、整体最优匹配、
  可区分的交叉运动、无效输入和重置。已用 Roslyn 3.11 / .NET 8 编译并执行实际生产类，
  70 项检查通过；数学替身不代表 Unity 组件或 MediaPipe 原生库已运行。
- `Tools/Faceless/verify_face_renderer.cs` 编译执行实际 `FacelessFaceRenderer.cs`，
  98 项检查通过：六人独立计时、材质与纹理历史，丢失暂停与重新采样、延迟重播、
  换人重置、生长进度、分配失败重试和资源释放。此测试替换 Unity/GPU 边界，
  不验证实际图形驱动、几何算法或摄像头。
- `verify_skin.py --multiface-only` 在 Mesa 上运行实际皮肤与合成片元程序：
  六个人使用不同的合成肤色，各自重建并保留独立纹理。六层组合与单人最终结果一致，
  区域外像素误差为 0；不同入场进度误差小于 5e-6；移除一层后其余五人不变，
  被移除位置没有旧层残影；前景脸入场为 0 时能恢复自身原画面。
  这是标准脸模型与合成光照测试，不代表六个真人的检测结果。
- Unity 6000.0.58f2 / Mac Metal、Windows 图形后端、真人 6 人与实际帧率：待现场验收。

以下为此前皮肤算法与入场渐变的检查记录，不能替代本轮多人运行验证。

Tools/Faceless/verify_skin.py 将项目中实际片元函数体转换为 GLSL，通过 Mesa 执行。
以 skimage 的 NASA Eileen Collins 人像和 MediaPipe 检测点作为测试输入；
测试素材不随仓库提交。脚本依赖及调用参数见文件头。

检查内容包括片元程序编译执行、输出没有 NaN/Inf、关闭效果时原图不变、
人脸边界框之外的像素不变，以及眉眼、鼻子、嘴唇采样位置的来源权重为零。
它不测试 Unity 的 C# API 链接、UI 顶点处理、摄像头镜像链路或 Metal 编译。

内部接缝回归额外覆盖 39 个转头/俯仰姿态，比较原版外侧透明度、封闭孔内
覆盖和来源权重，并在鼻旁已定位的接缝注入亮点，使用独立旧取色公式进行
A/B 对照。另检查入场零进度仍为原图，历史色斑被限幅并逐帧消退。
`--temporal-only` 独立检查明暗、色相变化下补洞纹理不会产生颜色断层，
以及稳定帧不变；该测试在修复前可复现 0.02125–0.0425 的补洞边界色差。
原始像素栅格可能把极细的羽化通道误判为封闭孔，因此对剩余软接缝用四倍
采样密度复核，分别记录近乎不透明的闭缝和真实外连通道。未被保守核心
完整包围的位置保留原透明度，并验证原像素不再参与皮肤重建；本测试
不代表所有近乎不透明的软接缝都被提高到 100% 覆盖。
此项验证使用 10 个实际片元程序，以及仅供对照的 2 个旧取色程序。

新增的投影回归使用官方标准脸模型，覆盖左右转头至 ±85°、俯仰 ±25°、
歪头 ±30°、镜像和透视共 252 种组合。距离投影轮廓至少 2 像素的朝前
五官点最小覆盖率大于 0.999，轮廓外误差小于 1e-7，淡黑输出误差为 0。
这些是合成几何与 Mesa 测试，不代表真人在上述角度的检测精度或 Metal 验收结果。
标准脸拓扑来源及许可证见 THIRD_PARTY_NOTICES.md。

局部颜色回归使用已知左右、上下色差的合成皮肤场，并仅在应被排除的五官和
折痕位置绘制深色污染。对比开关局部光照引导后的核心、羽化边缘与梯度误差，
并检查四个区域之间的色差，避免以填平整张脸的方式通过高光测试。
这证明对测试场的渐变和接缝有改善，不能视为真人光照的精确重建。
新增折痕位置的源采样权重均为零；本轮共运行 10 个片元程序。

高光回归在固定解剖点 50/280（面中）、140/369（下巴）叠加局部亮斑。
一组比较新四区开关后的覆盖和残留亮度，另一组固定全部区域、仅开关高光
压缩，区分覆盖变化与取色变化。额外检查脸轮廓边缘没有新增覆盖，以及
左右转头 0/±45/±70 度时仍有可信脸颊色源，避免亮斑处理吞掉全部取色区。
数值与对照图由脚本输出到指定的工作目录；均为合成测试，尚未在 Metal 验收。

参考图是单张处理后的图像。实时输入中的极端侧脸、头发/手遮住五官、
眼镜、大面积胡须和强烈侧光尚未实现专门的分割与补全；
这些情况下可能仍有边缘痕迹或明暗差异。真实侧脸的鼻子外轮廓会保留；
如果需要连凸出的鼻子轮廓也消失，还需要改变几何轮廓和补全背景。
本版不能保证检测器在所有角度和遮挡下准确，也不是匿名化或隐私保护工具。
全员丢失时使用淡黑；其他人仍可见时，漏检者的旧图层会被隐藏，因此该人的
摄像头五官可能重新出现。完全遮挡、交叉换位与超过 6 人时不保证每人持续覆盖。
镜像、转头、离开再进入、多人互相遮挡、帧率和最终质感必须在实际展览电脑上继续验收。
